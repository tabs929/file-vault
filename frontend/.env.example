# Must be reachable from the *browser* (not from inside Docker).
# In local development this is http://localhost:8000.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Auth uses an httpOnly session cookie set by the API — no tokens in localStorage.
